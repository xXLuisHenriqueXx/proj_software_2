import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: "100%",
    paddingTop: 12,
  },
  containerContent: {
    rowGap: 32,
    paddingBottom: 120,
  },
});
